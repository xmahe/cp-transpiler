namespace Board {

enum UartState {
    kUartStateIdle,
    kUartStateBusy,
    kUartStateN,
}

class Uart {
public:
    u32 baud_rate;
    fn construct(u32 baud_rate) -> void;
    fn Write(u8* data, u32 length) -> i32;
}

}
