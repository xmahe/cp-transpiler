# Board Serial Example

This example is a paired-module sample.

- `board_serial.hp` and `board_serial.cp` share the same stem
- together they form one logical `c+` module
- the header file declares the API surface
- the source file supplies function bodies and executable behavior

Use the directory as the input root when transpiling this sample.
